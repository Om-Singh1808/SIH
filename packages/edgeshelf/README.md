# retailsense-edgeshelf

Edge shelf monitoring for RetailSense (agent A04). See module docstrings:
`coverage.py` (classical estimator), `state.py` (persistence state machine), `osa.py`,
`thumbs.py`, `scanner.py` (ShelfScanner + occlusion), `sku.py`, `sku_clip.py`.
