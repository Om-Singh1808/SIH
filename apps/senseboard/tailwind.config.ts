/**
 * Tailwind v4 is configured CSS-first in `src/index.css` (@theme). This file exists
 * only so editors/tooling that look for a config find the content globs.
 */
export default { content: ["./index.html", "./src/**/*.{ts,tsx}"] };
